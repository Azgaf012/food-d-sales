spring:
  datasource:
    url: jdbc:mysql://localhost:3306/salesfood
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

app:
  config:
    apiInventary: http://localhost:8081
    kardexOutEndpoint: /Kardex/register-out
